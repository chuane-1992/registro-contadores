name: registro_contadores
description: Aplicacion movil para registrar lectura de contadores de agua y luz.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  intl: ^0.18.1
  googleapis: ^11.4.0
  googleapis_auth: ^1.4.1
  fl_chart: ^1.1.1

  # camera: ^0.10.5 # Future use
  # permission_handler: ^10.2.0 # Future use

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/credentials.json

